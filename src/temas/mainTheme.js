import { createTheme } from "@mui/material/styles";

let mainTheme = createTheme({
  palette: {
    primary: {
      // aeditip green
      main: "#57A639",
    },
    secondary: {
      main: "#004d25",
    },
  },
});
export default mainTheme;
